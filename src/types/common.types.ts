export type carouselContent = {
  image: string,
  header: string,
  subHeader: string
}

export type match = {
  teamA: string,
  teamB: string,
  teamALogo: string,
  teamBLogo: string,
  datetime: Date
}